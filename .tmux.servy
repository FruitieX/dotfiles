source-file ~/.tmux.conf
set -g pane-active-border-fg white
set -g pane-active-border-bg default
set status off
respawn-window -k 'weechat -r "/layout apply servy;/bar hide status"'
#splitw -v -p 10 -t 0
#selectp -t 1
#clock-mode
